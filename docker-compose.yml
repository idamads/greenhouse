version: "3.9"

services:

  greenhouse-dashboard:

    build: .

    container_name: greenhouse-dashboard

    ports:
      - "5000:5000"

    restart: unless-stopped

    volumes:
      - ./data:/app/data
